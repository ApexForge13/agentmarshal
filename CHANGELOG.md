# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Scope Contract artifact format (`spec/v0.1/scope-contract.schema.json`)
- Audit record companion schema (`spec/v0.1/audit-record.schema.json`)
- Composite predicate dispatch substrate (`lib/authzen/composite-dispatch.ts`): registry, Ajv-validated input schemas, fail-safe `isAllowable()` policy that treats `result: 'stub'` as non-allow
- `EvalContext` type (`lib/authzen/eval-context.ts`) carrying `tenant_id`, `agent_id`, `request_id`, and an `AuditEmitter` hook to composite predicates
- `composite_evaluations` field on `EvaluationResult` and `composite_checks` field on `ScopeRule` for declaring composite predicate invocations in Scope Contracts
- TCPA composite predicate library under `lib/compliance/predicates/tcpa/`: 6 predicates (2 stubs — `tcpa_dnc_registry_clear`, `tcpa_revocation_honored` — deferred to Bright Data integration day, 4 real checks) covering 47 CFR §64.1200(b)–(c) requirements; federal quiet-hours default with state-table integration point for the launch states
- CAN-SPAM composite predicate library under `lib/compliance/predicates/canspam/`: 6 predicates (1 stub `canspam_unsubscribe_mechanism_working` deferred to Bright Data integration day, 5 real checks) covering 15 USC §7704(a)(1)–(5)
- Explicit `registerAllTcpaComposites()` and `registerAllCanspamComposites()` registration functions for tree-shake resistance and grep-ability, with module-load side-effect calls preserving bare-import behavior
- Symmetric registry smoke tests under `tests/predicates/{tcpa,canspam}/`
- `docs(spec): agent inventory and layered contract model` (`spec/v0.1/agents.md`)
- `docs(spec): new predicate inventory (20 predicates queued for implementation)`
- `docs(spec): buffer-driven pull controller and daily cost report template`
- `docs(spec): six-loop self-improvement framework`
- `docs(spec): BD integration model with tiered enrichment depth`
- `feat(compliance): sourcing predicate stubs (8 predicates - 5 BD provenance, 3 sourcing) under lib/compliance/predicates/sourcing/`
- `feat(compliance): operational predicate stubs (7 predicates - 3 operational, 4 pipeline control) under lib/compliance/predicates/operational/`
- `feat(compliance): voice predicate stubs (4 predicates - runtime checks complementary to TCPA declarative checks) under lib/compliance/predicates/voice/`
- `feat(compliance): internal audit envelope (record_type: 'internal_audit') - schema + builder + spec doc, addresses agents.md §7.5 forward-reference`
- `feat(compliance): SMS deferred stub (sms_express_written_consent_recorded) under lib/compliance/predicates/sms/ — completes the 20-predicate v0.2 stub set (32 composites total registered)`
- `feat(access): evaluation endpoint emits a signed Compliance Receipt or Internal Audit envelope on every evaluation, attached under response.record with a record_type discriminator. Emission kind is selected by subject.type via lib/access/emission-policy.ts per agents.md §1 (CampaignManager/ResponseHandler/Voice → compliance_receipt; LeadScraper/Validator/InboxAllocator/Personalizer/COO/InboxProvisioner/RegulatoryMonitor → internal_audit; unknown → internal_audit with COO envelope fallback). Chain tracking deferred — previous_*_hash always null in v0.2.`
- `chore(routing): canonical AuthZEN 1.0 path /access/v1/evaluation rewrites to /api/access/v1/evaluation in next.config.ts (Day 5 deferred item closed)`
- `feat(authzen): file-backed Scope Contract loader with schema validation + in-memory caching. Loads from data/contracts/<id>.json, resolves agent→contract via data/agent-contract-map.json, falls back to STUB_PERMISSIVE_ALLOW on miss/error. New helpers: loadContractFromDisk, resolveContractIdForAgent, clearContractCache, loadAllContractsForStartup. Compiled Ajv validator at lib/authzen/contract-schema.ts.`
- `feat(data): seed Scope Contracts (outreach_v1, voice_v1, sourcing_v1, operational_v1) under data/contracts/ + data/agent-contract-map.json with 10 representative agent mappings (voice-001, leadscraper-001, validator-001, coo-001, inbox-allocator-001, campaign-manager-001, response-handler-001, personalizer-001, inbox-provisioner-001, regulatory-monitor-001).`
- `feat(compliance): governance predicate suite (5 REAL composites — first non-TCPA/CAN-SPAM real composites in v0.2 registry) under lib/compliance/predicates/governance/. Covers cross_tenant_isolation_enforced, action_scope_within_contract, spend_within_cap, agent_role_authorized_for_action, and input_injection_pattern_clear. Brings total composites to 37 (12 TCPA/CAN-SPAM real + 20 stubs + 5 governance real). Endpoint route registers the new domain via side-effect import.`
- `feat(benchmark): adversarial-pattern benchmark suite — 20 scenarios (15 adversarial + 5 legitimate) across 5 governance categories, three evaluation tracks (no-governance / naive / AgentMarshal). Auto-generates reports/benchmark.md from runner output. Integration test asserts headline catch rates (Track A 0/15, Track B 2-5/15, Track C ≥13/15, all 0 false positives). Closes the submission's 'no benchmark numbers' soft spot. Adds setContractOverride to lib/authzen/contracts.ts to support per-scenario inline-contract injection through the real loader; production code MUST NOT depend on it. CLI entry pnpm benchmark shells out to vitest because the in-process invocation chain transitively requires canonicalize (ESM-only) which tsx-CJS cannot resolve.`
- `docs(spec): refresh spec/v0.1/README.md to include all current spec artifacts (scope-contract, audit-record, compliance-receipt + md, internal-audit-record + md, agents.md) with explicit audit-record vs internal-audit-record distinction`
- `docs(spec): tier-metadata call-site contract note in agents.md §5.5 — clarifies that Tier 1/2/3 enrichment depth is surfaced by LeadScraper/Personalizer callers, not by individual composite predicates`
- `feat(voice): inbound voice agent scaffold with Vapi webhook integration, ElevenLabs TTS via Vapi voice config, OpenAI Whisper transcription via Vapi transcriber config, state-transition-triggered mid-call Marshal evaluation. Demonstrates consent-revocation arc: caller revokes recording consent mid-call, Voice agent's continued record_call attempt is caught by Marshal, signed Compliance Receipt emitted, call recovers gracefully. Upgrades voice_recording_consent_state_resolved from Bubble 3 stub to real composite. Sample agent in agentmarshal repo for hackathon demo; production architecture has voice agent in echo-os (documented in lib/voice/README.md).`
- `feat(verify): public Compliance Receipt + Internal Audit verification tool at /verify. Paste any receipt JSON, the tool verifies the Ed25519 signature against AgentMarshal's published public key (also served at /api/verify/public-key as JWK + hex) and surfaces the receipt's structured fields. Reuses the same JCS canonicalization (RFC 8785) and Ed25519 library as receipt emission — no new crypto. Includes pre-generated example receipts (valid Compliance Receipt, valid Internal Audit, tampered receipt). Demonstrates AgentMarshal's actual differentiator: third-party-verifiable, cryptographically-attestable agent decisions — independently checkable without trusting Marshal at all.`
- `feat(timestamp): RFC 3161 external timestamp anchoring for signed receipts via FreeTSA. Every Compliance Receipt and Internal Audit envelope now carries an optional TSA-signed timestamp token proving WHEN the receipt was issued, independent of AgentMarshal's clock or records. Verifier reports timestamp validity alongside signature validity at /verify and via /api/verify/receipt. Degrades gracefully if TSA is unreachable (receipt still signed, just not externally timestamped). Single TSA for v0.2 hackathon; redundancy roadmap documented in lib/compliance/timestamp/README.md. Directly addresses the failure mode in OFAC v. TradeStation Securities (Mar 2026, $1.1M settlement): when regulators examine systems years after the fact, every receipt now has third-party-anchored proof of issuance time, making the 'we have to reconstruct what happened from logs' failure mode structurally impossible.`
- `feat(benchmark): audit-trail tampering scenario suite (Bubble 12) — 5 new scenarios testing the audit-evidence layer (signed receipts, hash chains, RFC 3161 anchors, engine-independent verification). Threat model: operator adversary faking their own audit trail. AgentMarshal catches 5/5; Cedar and OPA score 0/5 structurally (no equivalent artifact in their data model — see Spike G). Adds verifyChain (lib/verify/verify-chain.ts) for sequence linkage verification, and a timestamp/issued_at cross-check (5-minute tolerance) inside verifyReceipt to catch backdated receipts. Deterministic fixture generation via scripts/generate-audit-trail-fixtures.mts (seed-derived forged Ed25519 key; replayed FreeTSA token), with a Section 2 report matrix contrasting AgentMarshal vs Cedar vs OPA on capabilities structurally beyond general-purpose policy engines. Includes a Bubble 11 fixture re-capture to align issued_at with the cryptographic genTime — a necessary precondition for the backdating cross-check; production code already signs-then-stamps within milliseconds, so the prior fixtures had a ~22.5h artificial gap that the new check would have flagged as a false-positive.`
- `feat(benchmark): trading desk scaffold + entity_not_sanctioned composite (Bubble 13). Lays the OFAC-sanctions scenario surface for the v0.2 hero arc — TradingAgent / ResearchAgent / RiskAgent / ExecutionAgent fleet, shared trading_v1 Scope Contract, entity_not_sanctioned composite reading regulatory_state.ofac_sdn_list from action_properties (same pattern as voice_recording_consent_state_resolved in Bubble 9). Four seed scenarios: ExecutionAgent denied on a sanctioned counterparty (the writeup's hero), three legitimate permits exercising the composite across the other agent roles. Composite returns an unresolved state when the SDN list isn't injected — production wiring (live OFAC SDN feed via Bright Data) replaces the fixture-injected stub in the next workstream. Anchor case: OFAC v. TradeStation Securities (Mar 17 2026, $1.11M, 481 violations across 8 months of silent control failure) — signed + timestamped + chain-linked receipts make that exact failure mode structurally impossible. Also relocates Spike G's Cedar/OPA capability analysis from /tmp to docs/spikes/cedar-opa-spike.md so the Section 2 footnote in reports/benchmark.md resolves for external readers.`
- `feat(dashboard): trading desk dashboard v0.2 + production subject.type resolver (Bubble 14). Replaces the v0.1 mike-cortez fleet view at demo.agentmarshal.dev with a 4-agent trading desk view (TradingAgent / ResearchAgent / RiskAgent / ExecutionAgent) — fleet panel, live activity feed, regulatory state panel (OFAC SDN status), receipt viewer with /verify integration, and a "Run demo sequence" trigger that fires the four trading scenarios through the real /api/access/v1/evaluation endpoint. Also extends loadContractForAgent with a strictly-additive subject.type fallback: instance-id keys (voice-001, outreach-001, …) resolve first — existing voice / outreach / operational behavior is byte-identical — and Bubble 13's type-name keys (TradingAgent, ResearchAgent, RiskAgent, ExecutionAgent) in agent-contract-map now resolve through the production path, with no setContractOverride in any non-benchmark dispatch. The feed source and regulatory snapshot are abstracted behind swappable providers so the Bright Data live OFAC feed and future feed persistence are provider swaps, not panel/feed rewrites. Retires the orphaned v0.1 dashboard components (Dashboard, AgentCard, ActivityFeed, AuditLog, HeroBlockCard, EscalationModal, PolicyEditor) + lib/dashboard-helpers. Production-shaped from the first commit; the dashboard is the hackathon submission's visual centerpiece and the Series A asset's primary surface.`
- `feat(dashboard): three-state outcomes + ambient feed + sidebar pages + custom input (Bubble 16). Extends AgentMarshal from binary permit/deny to permit/review/deny outcomes, matching the standard sanctions-screening workflow where exact matches deny outright, fuzzy substring matches block pending analyst review, and clean entities permit. CompositeResult adds review alongside pass/fail/stub; AuthZEN 1.0 binary at the API boundary preserved (decision stays boolean; new sibling review_required + review_reason fields carry the richer status). entity_not_sanctioned now returns review on substring matches against SDN region tokens (IRAN/CRIMEA/DPRK/etc.) - real fuzzy matching (Levenshtein, phonetic, alias resolution) lands in v0.3. Ambient feed simulation runs continuously in the browser, firing scenarios at jittered 3-8s intervals from a weighted pool (80% green, 15% yellow, 5% red) so the dashboard feels alive when judges or investors load it cold. Custom entity input lets users screen any counterparty against the SDN list through the real evaluation endpoint. New /audit-trail page shows all session receipts paginated; new /scope-contracts page shows trading_v1's full JSON, communicating the architectural artifact AgentMarshal's positioning is built around. Sidebar fleet placeholders (Outreach/Voice/Operational) visually disabled with "soon" indicators. Fleet card status badges fixed from stretched full-width boxes to inline pills. Receipt schema gains review_required + review_reason as optional fields, backward-compatible with all existing signed receipts.`
- `feat(dashboard): adopt Echo OS design language for the Trading Desk dashboard + /verify (Bubble 15). Replaces the Bubble 14 chrome with the Echo OS design system inherited from the broader Cortex product line — shell layout (200px collapsible sidebar + 48px topbar + main + 440px right rail), Inter + JetBrains Mono typography, burnt orange (#CC5500) accent, muted status palette, dense sharp-cornered layout. Sidebar surfaces Trading Desk active with placeholders for Outreach / Voice / Operational fleets, implying the broader Cortex system; topbar carries the AgentMarshal / Trading Desk breadcrumb, a search placeholder, and notification + settings actions; the page header carries the demo trigger as a primary-accent button. New 6-column metrics strip — Decisions today, Yellow flags (0 until Bubble 16's three-state lands), Hits caught, Avg eval time (captured client-side), Active agents, Audit chain length. Activity feed uses the .feed-row pattern with the 1.5s fresh-row highlight. Right rail is dual-purpose — default shows regulatory state (sanctions source, snapshot fingerprint, SDN entries), row-clicked shows receipt detail (verification status, full signed JSON, composite check breakdown), replacing Bubble 14's modal. Per the all-in adoption, Tailwind/PostCSS and the orphaned shadcn components/ui dir are removed (Echo OS is the sole design system) and the public /verify tool is restyled onto Echo OS classes (.textarea.mono paste box, .badge verdicts, .code JSON, .kv fields, .panel sections). Pure visual restructure — zero behavior changes, all 427 tests pass byte-identically, all Bubble 14 functionality preserved (live evaluation through the real endpoint, signed + timestamped receipts, /verify handoff via localStorage, paste-and-verify, example loaders). Establishes cross-product brand consistency with Echo OS / TextThread — AgentMarshal now reads as a product in a coherent portfolio rather than a one-off project.`
- `feat(mcp): MCP proxy foundation + first BD tool (SERP API) wired end-to-end (Bubble 17). AgentMarshal now runs its own Model Context Protocol server at /api/mcp/v1 (Streamable HTTP transport via the official @modelcontextprotocol/sdk Web-standard transport, stateless JSON mode): agents point their MCP client at AgentMarshal, every Bright Data tool call is evaluated against the agent's Scope Contract, and approved calls forward to BD with a signed receipt capturing the call. Scope Contracts gain an optional top-level bd_permissions array (the MCP proxy in lib/mcp/govern.ts consults it; the declared_scope evaluator ignores it): per-call rules carrying match (service/tool/parameter predicates supporting equals, in, exists, matches, domain_in), constraints, composite_checks (plain string ids), and decision permit; first matching and passing rule wins, no matching rule is an implicit deny that still emits a signed receipt. New trading_v2 contract adds one adverse_media_serp rule, and TradingAgent remaps to trading_v2 (ResearchAgent, RiskAgent, ExecutionAgent stay on trading_v1). New lib/bd/ Bright Data SERP client (Direct API POST to api.brightdata.com/request with brd_json=1, reading BRIGHTDATA_API_TOKEN + BRIGHTDATA_SERP_ZONE). Two BD-governance composites (bd_service_authorized, bd_query_purpose_matches) plus a serp_adverse_media_search MCP tool that governs the call, runs the BD search, and fingerprints the response (sha256) into a bd_call audit entry. Compliance Receipt and Internal Audit envelopes gain an optional signed bd_calls[] array (tampering with any bd_call breaks the signature; absence keeps every pre-Bubble-17 record byte-identical), and /verify surfaces it. The entity_adverse_media_check_v0 composite makes a governed SERP call through the proxy during a TradingAgent evaluation and records the resulting bd_call into the signed Internal Audit record (v0 returns pass regardless of results; real adverse-media interpretation lands in Bubble 19). Web Unlocker and the remaining 5 BD services come in Bubble 18; multi-source sanctions and adverse-media scoring in Bubble 19. Governance and the full MCP proxy path are verified end-to-end (tools/list, governed deny, permit with a mocked BD call, live HTTP route over Next.js); live SERP execution requires a real SERP API zone (the seeded BRIGHTDATA_SERP_ZONE is a datacenter proxy zone that BD blocks for Google, so the one real-call checkpoint is gated until a SERP zone is configured).`
- `feat(mcp): Web Unlocker + Crawl API governed through the MCP proxy (Bubble 18). Two more Bright Data products join the Bubble 17 proxy pattern, governed by the same bd_permissions rules and audited via the same signed bd_calls[] receipt field; architecturally identical to Bubble 17 (pattern replication, not new infrastructure). The BDService enum gains crawl_api (scope-contract.schema.json + types/authzen.ts). lib/bd/ extracts a shared bdDirectApiRequest transport (bearer auth, the HTTP-200 x-brd-* upstream-error envelope Bubble 17 discovered, empty-body guard) and adds bdWebUnlockerFetch (Direct API /request, format raw, returns the unlocked HTML) and bdCrawlScrape (datasets/v3/scrape with notify=false, flattening BD per-URL response array into results.items). Two new governed MCP tools: unlock_news_article (Web Unlocker, for paywalled financial news during adverse media review) and crawl_article_content (Crawl API, clean article extraction designed to chain after a SERP search). New bd_domain_in_scope composite enforces a per-rule URL hostname allowlist (wildcard subdomains via *.host, conservative on the bare apex) as an explicit, audited check paralleling govern.ts match-level domain_in predicate. trading_v2 adds adverse_media_unlock (Web Unlocker, financial-news domain allowlist) and adverse_media_extract (Crawl API) rules, declaring the SERP then Unlock then Crawl adverse-media pipeline. Web Unlocker and Crawl API are verified live end-to-end through the HTTP transport (governed deny on a non-allowlisted domain with no BD call; permit plus real fetch plus sha256 response fingerprint); gated live round-trips added to tests/integration/mcp-bd-roundtrip.test.ts. Scraping Browser is deferred to Bubble 20: that product is driven over a CDP/WebSocket browser connection (Puppeteer/Playwright), not the Bearer /request API (a browser zone returns HTTP 403 on /request), so it needs a new dependency and credential model out of scope here. Real composite interpretation (entity_no_adverse_media returning pass/review/fail from BD response content) lands in Bubble 19.`
- `feat(compliance): real adverse media scoring (entity_adverse_media_check v1) (Bubble 19). v1 replaces the Bubble 17 v0 pass-always scaffold with actual pass/review/fail interpretation: during a trading evaluation it chains a governed SERP search and a Crawl API extraction of each top result through the MCP proxy, then scores the extracted article content against an eight-term financial-crime keyword list (fraud, investigation, indictment, money laundering, sanctions violation, regulatory action, asset freeze, criminal charges). It counts DISTINCT keywords present (case-insensitive) and applies per-contract thresholds from the composite static input (review_threshold default 1, fail_threshold default 3; search_query_template, max_results_to_extract, and keyword_list also configurable) so contracts tune sensitivity without code changes. Each governed call pushes its bd_call onto the receipt, so a signed record now captures the full screening chain (1 SERP + up to N Crawl calls, all permit + response-fingerprinted). Policy is best-effort and non-blocking: when screening cannot execute (no BD credentials, SERP denied or errored, or every extraction fails) the composite returns pass and records the attempt for audit rather than blocking the trade on an infrastructure gap; only retrieved-and-scored content can yield review or fail. The default query is quote-anchored on the entity name (a live check showed an unquoted keyword pile surfaces other entities fraud coverage and false-positives a clean counterparty); keyword scoring stays heuristic and false-positive-prone, so LLM-based interpretation is the parked v0.3 enhancement. v0 (entity_adverse_media_check_v0) is deprecated but kept registered for backward compat (removal in v0.3); trading_v2 migrated its declared_scope composite check from entity_adverse_media_check_v0 to entity_adverse_media_check. Hermetic unit tests mock the SERP and Crawl chain; a gated live test verifies the real chain end-to-end, asserting chain integrity and a real verdict rather than the data-dependent score.`
- `feat(mcp): governance-wrapped MCP Server passthrough to Bright Data's MCP (Bubble 20 Phase B). AgentMarshal now fronts Bright Data's hosted MCP server (https://mcp.brightdata.com/mcp, Streamable HTTP, authenticated with the existing BRIGHTDATA_API_TOKEN passed as a token query param, not a Bearer header) as a transparent governance layer. A single generic tool, bd_mcp_passthrough, forwards an arbitrary MCP tools/call to BD AFTER the agent Scope Contract governs it, so any tool BD adds to its catalog is auto-governed with no code change here. lib/bd/mcp-passthrough-client.ts wraps the MCP SDK client over a Streamable HTTP transport (connect injectable for hermetic tests); the requested BD tool rides in parameters.bd_tool_name and a new bd_passthrough_tool_in_allowlist composite enforces the matched rule bd_tool_name allowlist (an explicit, audited parallel to the match-level in predicate). trading_v2 adds a bd_mcp_governed_passthrough rule allowlisting the curated web-access tools (search_engine, scrape_as_markdown, and their batch variants); ask_brightdata_assistant is deliberately excluded so a passthrough for it is governance-denied. The wrapped call and BD response are captured in a mcp_server bd_call audit entry. Verified live end-to-end (tools/list plus a governed search_engine forward). Bright Data pre-built Web Scraper API datasets (e.g. LinkedIn People) are out of scope for v0.2: priced as enterprise dataset subscriptions, not per-call API access.`
- `feat(mcp): Scraping Browser via puppeteer-core + CDP/WebSocket (Bubble 20 Phase C). Resurrects the ubo_registry_lookup rule dropped in Bubble 18, now with the correct transport: BD's Scraping Browser is a remote browser driven over CDP (a browser zone returns 403 on the Bearer /request API, per Bubble 18), so lib/bd/scraping-browser-client.ts connects puppeteer-core (the connect-only variant, no bundled Chromium) to wss://<BROWSER_USER>:<BROWSER_PASS>@brd.superproxy.io:9222, navigates, captures page.content(), and always disconnects (never closes BD's remote browser) via try/finally; connect is injectable for hermetic tests. A new browse_registry_page MCP tool (purpose registry_lookup, optional wait_for_selector) records a scraping_browser bd_call; trading_v2 re-adds ubo_registry_lookup (service scraping_browser, url domain_in find-and-update.company-information.service.gov.uk plus *.opencorporates.com, gated by bd_domain_in_scope). Credentials are the zone username/password (BRIGHTDATA_BROWSER_USER + BRIGHTDATA_BROWSER_PASS), a different model than the API token + zone name. Verified live end-to-end (example.com CDP transport sanity plus a governed OpenCorporates registry render). NOTE: Bright Data's network policy blocks Government domains, so UK Companies House is unreachable via Scraping Browser despite being in the allowlist (governance permits; BD refuses the fetch), so the live path uses OpenCorporates, a non-government registry aggregator. Six Bright Data products are now governed end-to-end: SERP, Web Unlocker, Crawl API, MCP Server passthrough, Scraping Browser, plus the implicit proxy network.
- `feat(compliance): LLM-based adverse media scoring via AI/ML API (Bubble 22). Upgrades entity_adverse_media_check from the Bubble 19 distinct-keyword heuristic to LLM interpretation through AI/ML API, an OpenAI-compatible gateway (POST https://api.aimlapi.com/v1/chat/completions, Authorization: Bearer ${AIML_API_KEY}, default model openai/gpt-4.1-mini). The LLM reads the SERP+Crawl-extracted article content and decides pass/review/fail PLUS produces a one-sentence reasoning string that lands verbatim in the composite reason field. That string flows into the signed Compliance Receipt or Internal Audit envelope, so every screening verdict now carries a human-readable, cryptographically-attestable justification (e.g. "SEC indicted Helix Bridge Capital Partners for fraud and money laundering, along with regulatory actions including asset freezes and operating bans") instead of a keyword count. Fixes the documented Bubble 19 false-positive: live testing on a name-collision sample (article about Meridian Group Holdings, screening Meridian Corp) the keyword scorer would false-positive on returned a clean pass with reasoning "The adverse media concerns a different entity named Meridian Group Holdings, not Meridian Corp." This was the parked Bubble 19 v0.3 item pulled forward to land it inside the hackathon window. New static-input fields on the composite: scoring_mode (default llm_with_keyword_fallback, alternatives llm_only and keyword_only) and llm_model (default openai/gpt-4.1-mini). New lib/llm/ generic OpenAI-compatible chat-completions client (timeout via AbortController, fetchImpl injectable for hermetic tests, captures meta.usage.credits_used and meta.usage.usd_spent for cost telemetry, throws LlmConfigError when AIML_API_KEY is missing and LlmRequestError on non-2xx, timeout, empty body, or non-string content). lib/compliance/predicates/trading/adverse-media-llm-scorer.ts builds the prompt (system + user template), calls the gateway with response_format json_object and temperature 0, defensively validates the structured output (verdict in {pass, review, fail}, non-empty reasoning, concerns array), throws LlmRequestError on any structural problem, and truncates content over a 6000-char budget (recorded in details). Default mode falls back to the keyword scorer on any LLM failure (no key, request error, malformed output) and records llm_fallback: true with llm_error: string, so screening degrades safely when the LLM is unavailable and the hermetic suite stays green without an AIML_API_KEY; llm_only on failure returns pass with screening_unavailable: true matching the existing can't-screen best-effort policy; keyword_only forces the heuristic path. All Bubble 19 best-effort semantics preserved: no entity to screen stays a stub pass; SERP unavailable stays a non-blocking pass with screening_unavailable: true; all Crawl extractions failing stays a non-blocking pass with screening_incomplete: true; only retrieved-and-scored content can yield review or fail. Hermetic unit tests cover the AI/ML client (10 cases including timeout via AbortController), the LLM scorer (11 cases per verdict plus malformed-output throws plus content truncation), and the composite under all three scoring modes plus the fallback transition (29 new tests total, full suite 586 passing 12 skipped from 557 + 12). A gated integration test at tests/integration/adverse-media-llm-live.test.ts (gate on AIML_API_KEY) exercises the real gateway on three crafted fictional samples (clean entity, adverse entity, name-collision) with sane-bucket assertions rather than pinned verdicts, since LLMs reason. Powered by AI/ML API (hackathon technology partner, Best Use of AI/ML API prize hook satisfied via Track 3 Security and Compliance plus Track 2 Finance and Market Intelligence). Dashboard rendering of the LLM reasoning string in the audit-trail UI is the Bubble 21 polish item now landing on top of the richer output.
- `test(compliance): prove LLM adverse-media reasoning survives into the signed audit record (Bubble 23). Closes the Bubble 22 coverage gap. Bubble 22 tested the LLM scorer and the composite at the result/details level but never exercised the full path from LLM reasoning string through buildRecord, Ed25519 signing, and verification. Bubble 19 proved this class of path can have gaps hermetic tests miss (the crawl_api enum bug only surfaced on a real eval). Adds one hermetic regression test in each of tests/compliance/internal-audit/builder.test.ts and tests/compliance/receipt/builder.test.ts, alongside the Bubble 19 enum regressions. Each test constructs a composite_evaluation shaped like the LLM scorer output (result fail, reason a representative reasoning sentence, details carrying llm_reasoning, llm_concerns, llm_model, scoring_path llm, scoring_mode llm_with_keyword_fallback, plus the cost telemetry fields), flows it through the corresponding builder, and pins five invariants: (a) the reasoning sentence, every concern string, the model identifier, the scoring mode, and the scoring path all appear in the RFC 8785 canonical body the signature covers, (b) the Ed25519 signature verifies against that canonical body using the test-local public key, (c) audit_hash / receipt_hash recomputes from body plus signatures, (d) tampering with the reasoning sentence flips signature verification (the LLM fields are inside the signed bytes, not appended after), (e) a follow-on record carries previous_audit_hash / previous_receipt_hash and its own hash recomputes. Live-confirmed end-to-end against the real production endpoint (POST /api/access/v1/evaluation with subject.type TradingAgent, action propose_trade, AIML_API_KEY and BD credentials loaded): the returned Internal Audit record's evaluation.composite_evaluations[1].reason is the LLM's reasoning sentence verbatim, and POST /api/verify/receipt against the unmodified record returns verified true ("signature valid: receipt is authentic and unmodified") with a valid FreeTSA timestamp anchor. The production verifier path (lib/verify/verify-receipt.ts -> verifyReceipt) reads the published public key from disk so tmpdir-backed test keys cannot exercise it hermetically; the live capture covered that path. The claim that every signed audit record carries the LLM justification is now a tested invariant rather than an architectural inference. Hermetic suite 588 passing 15 skipped (from 586 + 12; the 15 skipped includes 3 newly-gated Bubble 22 LLM live tests). No production code changed.`
- `feat(dashboard): demo-recording polish, persisted-receipt browser, tamper-edit UI (Bubble 21). New /receipts page in the Echo OS dashboard shell (added to the Sidebar System nav): a left-rail list of signed records (persisted demo fixtures read from disk plus this session live decisions from the shared feed store, newest-first, de-duplicated by id) feeding a polished detail panel, with the first DENY selected by default so the hero record is on screen at load. The detail panel renders the LLM adverse-media reasoning sentence prominently (verdict-colored left border), color-codes composite checks (pass green, review amber, fail red), lists governed Bright Data calls per service with target (URL or search query), sha256 response fingerprint, latency, and governance result, gives governance-deny events (the ask_brightdata_assistant passthrough refusal) a distinct policy-refused treatment, links the hash chain via clickable previous-receipt navigation, and renders best-effort states (screening_unavailable, screening_incomplete, keyword fallback, empty bd_calls) as distinct clean states rather than errors. Every model-emitted string (reasoning, concerns, reasons) renders through React default escaping; no dangerouslySetInnerHTML on any path reachable from LLM output. The cold-open feature is the tamper-edit surface: an editable canonical-JSON view of the selected record with Re-verify and Reset, beside a one-click "Verify this receipt" button on the unmodified record. Both POST the current bytes to /api/verify/receipt (the real verifier on real, sometimes-modified bytes; no simulation, no canned tamper variants) and share one oversized green VERIFIED to red TAMPERED result banner; editing one character of the signed reasoning flips the verdict to "signature mismatch", unmodified bytes verify clean with the FreeTSA timestamp anchor reported. Phase 0 captures four real signed Internal Audit fixtures under data/demo-receipts/ via a gated capture tool (tests/demo/capture-demo-receipts.test.ts, gate CAPTURE_DEMO_RECEIPTS plus AIML_API_KEY): a real hash chain (Northwind clean genesis, then Helix Bridge adverse-media fail, then Meridian name-collision pass, then a governance-deny passthrough), each carrying a live AI/ML API verdict and reasoning, a real FileKeyProvider Ed25519 signature, and a real FreeTSA RFC 3161 timestamp anchor, all verifying true. Because a fictional counterparty has no live web coverage for SERP to index, the adverse-media article content is supplied directly to the LLM scorer (the verdict and reasoning are a real model call) and that provenance is documented inside the signed composite details, so the record never misrepresents how it was produced and the note is itself tamper-evident. No backend changes (no new predicates, integrations, schema, or builder edits); the /api/verify/receipt endpoint was already body-driven so the tamper-edit needed no security-surface change. Echo OS design language preserved (one scoped .rcpt-* CSS block over existing tokens, no new style libraries or color palette). tsc and pnpm build clean; hermetic suite 588 passing preserved, 16 skipped (the new gated capture tool contributes one skipped test alongside the existing gated network tests).`

### Changed
- TCPA composite predicate names normalized to predicate-attribute convention to match CAN-SPAM and produce naturally-readable receipt entries (`tcpa_quiet_hours_check` → `tcpa_quiet_hours_respected`, etc.; full map in this commit). Behavior unchanged.
- `lib/compliance/predicates/tcpa/index.ts` refactored to export `registerAllTcpaComposites()` alongside the existing side-effect import. Bare-import behavior unchanged.
- `chore(spec): replace "Day 6" relative references with "Bright Data integration day" semantic anchor across predicate stub comments (tcpa, canspam), agents.md (§2.1, §2.2, §7.2), and compliance-receipt schema + spec doc`
- `refactor(tests): sourcing predicate tests refactored from 1-block to 3-block pattern for parity with operational/voice/sms convention - codebase test-shape consistency, no behavior change`
- `fix(spec): extend scope-contract.schema.json ScopeRule definition to include composite_checks (was implemented in lib/authzen/evaluate.ts + types/authzen.ts since Day 3 Bubble 2 but missing from the normative schema; partial closure of schema-verification Issue queue). New CompositeCheck $def with { predicate: string, input: unknown } shape matching impl.`
- `feat(authzen): EvalContext gains optional action_properties, populated by evaluateRequest from the AuthZEN request's action.properties. Lets composites whose verdict depends on request-time state the contract cannot know at issuance (e.g. mid-call consent) read it without setContractOverride. Additive + optional; existing composites unaffected. First consumer: voice_recording_consent_state_resolved (Bubble 9).`

### Deprecated
- v0.1 manifest format — will be superseded once Scope Contracts replace it end-to-end.
